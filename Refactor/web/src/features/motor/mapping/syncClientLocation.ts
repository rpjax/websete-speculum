/**
 * Thin re-export — Sessions owns address-bar sync for live browsing.
 * MotorEngine keeps importing from this path.
 */
export { syncClientLocation } from '@/features/sessions/live/syncClientLocation'
