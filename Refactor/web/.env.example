# Same-origin (dockup / production): leave VITE_API_URL unset.

# Vite dev against dotnet on :8080 (cross-origin):
# VITE_API_URL=http://localhost:8080

# Mock mode — run the SPA with simulated API data, no backend needed:
# VITE_MOCK=1
