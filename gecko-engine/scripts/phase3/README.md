# Phase 3 — Engine sim

Gate: `pwsh -File scripts/phase3/run.ps1` from `gecko-engine/`.

Accepts A1–A7 (hosts no root special case, three viewports, process kill order,
NavigationState table, illegal transition, invariants, composition root).
