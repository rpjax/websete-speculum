/**
 * @deprecated Import from `./model/buildNarrative` — kept as a thin re-export during migration.
 */
export {
  SPAN_ABANDONED_EVENT,
  compareEvents,
  orderEvents,
  buildSpans,
  buildStories,
  type Span,
  type Story,
} from './model/buildNarrative'
export type { SpanStatus } from './model/narrativeTypes'
