/**
 * Video Streaming mirror surface (JPEG screencast canvas).
 * Re-exports the shared SessionViewport implementation.
 */
export {
  SessionViewport,
  type SessionViewportProps,
} from '../SessionViewport'
