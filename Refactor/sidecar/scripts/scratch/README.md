# scratch/

Debug/repro scripts from past investigations. Not run in CI, not run in lab
gates, not referenced by `package.json` or any doc. Kept only as historical
reference in case a future investigation needs to consult how a prior one was
reproduced.

`diag/` holds the sidecar CDP/session repro scripts moved here on
2026-08-27 as part of the input-pipeline sanitize pass
(`docs/page-projection/spec/README.md` → input adapter unification, Fase 1.1).
