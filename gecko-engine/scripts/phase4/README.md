# Phase 4 — Producer

Gate: `pwsh -File scripts/phase4/run.ps1` from `gecko-engine/`.

Accepts A1–A12 (halt/resume, drain coalesce, field postcondition, sibling run,
emit purity, apply roundtrip, cold/resync, snapshots, Policy, Digest C++/TS,
OPEN-7/8, producer on sim).
