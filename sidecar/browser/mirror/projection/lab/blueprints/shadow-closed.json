{
  "id": "shadow-closed",
  "description": "Closed shadow NIT — explicit unsupported fail (PP-F-4)",
  "sessionPolicy": "cold",
  "fold": "shadow-closed",
  "queues": [
    {
      "name": "main",
      "actions": [
        { "id": "boot", "type": "boot", "params": { "url": "fixtures/shadow-closed.html" } },
        { "id": "wait-settle", "type": "sleep", "dependsOn": ["boot"], "params": { "ms": 800 } },
        { "id": "fold", "type": "fold", "dependsOn": ["wait-settle"], "params": { "ruleset": "shadow-closed" } },
        { "id": "writeDossier", "type": "writeDossier", "dependsOn": ["fold"] }
      ]
    }
  ]
}
