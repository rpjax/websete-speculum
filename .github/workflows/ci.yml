name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  dotnet:
    name: refactor-dotnet
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI noop — legacy refactor-dotnet retired"

  sidecar:
    name: refactor-sidecar
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI noop — legacy refactor-sidecar retired"

  web:
    name: refactor-web
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI noop — legacy refactor-web retired"

  page-projection-oracles:
    name: page-projection-oracles
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI noop — page-projection-oracles retired"

  compose:
    name: refactor-compose
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI noop — legacy refactor-compose retired"

  dockup:
    name: refactor-dockup
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI noop — legacy refactor-dockup retired"

  sessions-test:
    name: sessions-test
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI noop — legacy sessions-test retired"
