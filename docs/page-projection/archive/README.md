# Archive — pre-V4 PageProjection docs

**Do not implement from this directory.**

V4 (table / frame / two-phase apply / resync / no establish) is `docs/page-projection/spec/`. Start at [`../spec/README.md`](../spec/README.md).

These files are **provenance only**: historical contracts, the 2026-08-11 engine-redesign (including superseded §5.4–§5.7), the 2026-08-12 “buildable pack”, debate handoff, and duplicate copies that used to sit beside the live spec.

Decision content that still matters was **copied or indexed** into [`../spec/decision-log.md`](../spec/decision-log.md). Open bugs/gaps: [`../spec/open.md`](../spec/open.md).

Layout: `pre-v4/` (old contracts + implementation pack + coalesce/diff-*), `input-v2.md` + `input-unified-design-draft.md` (archived from `spec/` 2026-08-31), plus `engine-redesign.md`, `engine-redesign-extension.md`, `HANDOFF.md`, `RECONCILIATION.md`, `work-order.md`, `DECISIONS.md` (2026-08-12 pack table, verbatim).

If a live spec and a file here disagree, **the live spec wins.**
