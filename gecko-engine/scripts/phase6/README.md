# Phase 6 — Oráculo

Gate: `scripts/phase6/run.ps1`

Accepts A1–A10 (global freeze barrier, ida `d(VN)×d(VTR)`, volta 3 buckets +
ExclusionLedger, Verdict five fields, pure `IReconstructor`, injected defect,
ISA bytes identical with metrics off/on, `ProbeDisabled`, launch toggles).

No libxul. Sim only.
