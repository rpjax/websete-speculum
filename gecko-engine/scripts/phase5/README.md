# Phase 5 — Roteiro

Gate: `pwsh -File scripts/phase5/run.ps1` from `gecko-engine/`.

Accepts A1–A9 (record→check, two records identical, one format, §6,
schema refuse, diverge cause, recorder off, unknown event, blobs).
