# Fase 2 — Sessão

```
powershell -File gecko-engine/scripts/phase2/run.ps1
```

Entrega: ILink/IClock, ScriptedLink/ManualClock, Session, LinkWriter, Router, Correlations,
HeartbeatService, DeathDump. Aceite A1–A9 no binário `phase2_tests`.
