# Legacy MotorPerf (root Speculum.MotorPerf.Tests + motor-assert compose) is paused.
# CI is Refactor-only until Refactor/ is promoted to repo root. Reintroduce a
# Refactor capacity job here when needed; do not resurrect the root stack.
name: Perf

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy perf disabled
        run: |
          echo "Refactor-only CI era: root MotorPerf is not run."
          echo "Use workflow_dispatch only as a placeholder until a Refactor perf job exists."
