# Capacity / SLO jobs. Functional correctness stays in ci.yml.
# Reintroduce a capacity job here when needed.
name: Perf

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - name: Perf placeholder
        run: |
          echo "No capacity job wired yet."
          echo "Use workflow_dispatch as a placeholder until a perf job exists."
