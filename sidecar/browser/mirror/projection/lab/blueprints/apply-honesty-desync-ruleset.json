{
  "id": "apply-honesty-desync-ruleset",
  "description": "RULESET desync — inject RULE_SET on a grouping MediaRule (DOM client required)",
  "sessionPolicy": "cold",
  "fold": "apply-honesty-desync",
  "queues": [
    {
      "name": "main",
      "actions": [
        { "id": "boot", "type": "boot", "params": { "url": "fixtures/static-dom.html" } },
        { "id": "wait-settle", "type": "sleep", "dependsOn": ["boot"], "params": { "ms": 2500 } },
        {
          "id": "inject-ruleset",
          "type": "injectFrame",
          "dependsOn": ["wait-settle"],
          "continueOnFail": true,
          "params": { "kind": "ruleset" }
        },
        { "id": "fold", "type": "fold", "awaits": ["inject-ruleset"], "params": { "ruleset": "apply-honesty-desync", "kind": "ruleset" } },
        { "id": "writeDossier", "type": "writeDossier", "dependsOn": ["fold"] }
      ]
    }
  ]
}
